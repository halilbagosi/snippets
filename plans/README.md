# Implementation Plans

Generated by the improve skill on 2026-07-15, at commit `79537db3` (branch `snippet-live-previews-connections`). **Note:** the working tree had uncommitted changes when these plans were written; run each plan's drift check before executing. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Build/test commands used throughout (see plan 001):
`DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift build --build-path /tmp/snippets-build` / `swift test --build-path /tmp/snippets-test-build`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix README requirements; add repo CLAUDE.md | P1 | S | — | DONE |
| 002 | Trash/delete integrity (purge collections, honor permanent, surface saves) | P1 | M | — | DONE |
| 003 | Web preview CSP + navigation lockdown | P1 | M | — | TODO |
| 004 | Crash-safe Swift-preview dylib cache | P2 | S | — | TODO |
| 005 | Design doc: sandbox/contain Swift-preview execution | P2 | L (design M) | — | TODO |
| 006 | Characterization tests for App Intents | P1 | M | — | TODO |
| 007 | CI workflow (swift test + web build) | P2 | M | 001, 006 | TODO |
| 008 | Delete dead DesignSystem components | P2 | S | — | TODO |
| 009 | Incremental web-preview updates | P3 | M | 003 | TODO |
| 010 | Memoize gallery filtering/search | P3 | M | 002 (same file) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 007 (CI) needs 001's verified commands and is far more valuable once 006's suite exists.
- 009 modifies the same two preview files as 003 and must preserve 003's CSP and navigation delegate — land 003 first.
- 002 and 010 both edit `ContentView.swift` (different sections); run 002 first to avoid churn on line anchors.
- 005 is a design doc; it gates any future snippet **import/sharing** feature, and its outcome may relocate 004's cache logic (004 is still worth doing now).

## Direction findings (options recorded, no plans written)

- **Export/import**: full CRUD + intents but no backup/share path for a local-only store; `SnippetLinker.resolve` already computes the dependency closure an export would serialize. Blocked-ish on plan 005's trust-model decision for the import side.
- **Render previews to image**: three preview engines exist with no output consumer beyond the screen; per-engine snapshot is cheap (WKWebView `takeSnapshot`, Metal readback, `ImageRenderer`).
- **Token codegen for the React port**: generate Swift + TS tokens from one source (or a parity test) to enforce the "Swift is source of truth" claim in `web/design-system/README.md`.

## Findings considered and rejected (do not re-audit)

- Custom `isDeleted` shadowing `PersistentModel.isDeleted` (models + ~10 call sites): real but low-impact rename; do opportunistically during a future god-file refactor.
- God-file breakup (`SnippetGalleryView` 1521 / `ContentView` 1453 / `SnippetDetailView` 1035 lines): deferred — plans 002/010 extract testable pieces first; a full refactor plan should follow them.
- `SnippetEntity` fabricated UUID + un-backfilled collection filter in `FindSnippetsIntent`: latent; characterized (not fixed) by plan 006's tests.
- `MediaManager.resolvedURL` path-traversal guard: latent (filenames are app-generated UUIDs today); add the single-component check when import/sync work begins (plan 005 notes it).
- `stripModuleSyntax` blind string replacement in `WebPreviewHTMLBuilder`: real edge case, low frequency; proper fix needs tokenizing — revisit if users report corrupted previews.
- Metal shader runtime compilation: reviewed, low risk by design (GPU driver surface, no filesystem/network sink); optional compile watchdog only.
- Vendored React/Babel provenance manifest: worth a note, not a plan; fold into plan 007 follow-ups if desired.
- Dual build system consolidation: hazard is structural, no live drift found; plan 001's CLAUDE.md documents the registration rule, plan 005 may make the xcodeproj load-bearing (XPC target) — decide after 005.
- Unpinned web devDependencies / two React copies: not worth doing beyond documentation; `npm audit` clean.
- Preview build Task cancellation race (`SwiftPreviewHostView.run()`): real but low frequency (explicit multi-second builds); fix alongside plan 004 if convenient — kept out of scope there to keep it small.
- `SnippetEditorViewModel.hasUnsavedData` media identity comparison: LOW confidence, needs verification; investigate only if users report lost edits.

## Audit coverage note

Standard-depth audit, four parallel read-only agents (correctness, security, tests+debt, perf/deps/DX/docs/direction), all top findings re-verified against source by the advisor. Not audited: icon assets, `.xcodeproj` internals beyond build settings, untracked `Docs/`, runtime profiling of the two perf findings.
